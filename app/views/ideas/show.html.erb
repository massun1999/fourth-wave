<%= @idea.title %>
<%= @idea.text %>
<%= @idea.category.name %>
<%= @idea.format.name %>
<%= link_to @idea.user.nickname, user_path(@idea.user.id) %>

<%= link_to '購入する', idea_orders_path(@idea) %>

<%= link_to '削除する', idea_path(@idea), method: :delete %>

<%= link_to 'TopPage', root_path%>

